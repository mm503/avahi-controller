apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: avahi-controller
rules:
  - apiGroups: [""]
    resources: ["services"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
